# Docker Compose Environment Variables
# Copy to .env.docker and customize if needed (optional - defaults work out of the box)
# Usage: docker compose --env-file .env.docker up -d
#
# WARNING: Postgres credentials are set at FIRST VOLUME CREATION only.
# To change credentials after initial setup:
#   1. docker compose down -v  (DELETES ALL DATA)
#   2. Update this file
#   3. docker compose up -d

# =============================================================================
# PostgreSQL (Development)
# =============================================================================
POSTGRES_CONTAINER=vacay-postgres-dev
POSTGRES_USER=vacay
# Default: vacay (for local dev only - change in production!)
POSTGRES_PASSWORD=
POSTGRES_DB=vacay
DEV_DB_PORT=5433

# =============================================================================
# API Service (used by docker-compose.dev.yml)
# =============================================================================
API_CONTAINER=vacay-api
API_PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
