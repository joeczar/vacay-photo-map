# Remote Development Environment (connects to production DB via Tailscale)
# Copy to api/.env.prod and fill in values
# Run with: pnpm dev:prod

# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL (local dev server)
FRONTEND_URL=http://localhost:5173

# Production Database (via Tailscale)
# Get Tailscale IP with: tailscale status
DATABASE_URL=postgresql://vacay:YOUR_PROD_PASSWORD@TAILSCALE_IP:5432/vacay
DATABASE_SSL=false
DATABASE_POOL_SIZE=10
DATABASE_IDLE_TIMEOUT_MS=10000

# JWT Configuration (use same secret as production for token compatibility)
JWT_SECRET=YOUR_PROD_JWT_SECRET
JWT_EXPIRATION=1h

# WebAuthn Configuration (localhost for local dev)
RP_ID=localhost
RP_NAME=Vacay Photo Map
RP_ORIGIN=http://localhost:5173

# Local photo storage (for uploads during dev)
PHOTOS_DIR=/data/photos

# Proxy Configuration
TRUSTED_PROXY=false
