# Test Environment Configuration
# Copy this file to .env.test: cp .env.test.example .env.test
# These values are loaded automatically by test-setup.ts via bunfig.toml

# Database (must match docker-compose.yml)
DATABASE_URL=postgresql://vacay:vacay@localhost:5432/vacay

# JWT Secret (minimum 32 characters for HS256)
JWT_SECRET=test-secret-key-for-testing-only-minimum-32-chars

# WebAuthn / Passkey Configuration
RP_ID=localhost
RP_NAME=Test App
RP_ORIGIN=http://localhost:5173

# Photo Storage (R2 is mocked in tests, uses local fallback)
PHOTOS_DIR=./data/photos-test

# Node Environment
NODE_ENV=test
