{
  "name": "vacay-photo-map-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "dev:prod": "bun run --env-file .env.prod --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "migrate": "bun run scripts/migrate.ts",
    "seed": "bun run scripts/seed.ts",
    "reset-password": "bun run scripts/set-password.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.956.0",
    "hono": "^4.6.0",
    "jose": "^6.1.3",
    "postgres": "^3.4.7",
    "postgres-migrations": "^5.3.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "^1.3.3",
    "typescript": "^5.9.3"
  }
}
