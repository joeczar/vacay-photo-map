# Application Configuration
VITE_APP_URL=http://localhost:5173

# API Backend (Hono server)
# Change port to match api/.env PORT (default: 3000)
VITE_API_URL=http://localhost:3000

# CDN Configuration (optional, for production with Cloudflare Worker)
# Leave empty to use API routes for images (development mode)
# Set this to your Cloudflare Worker URL when deployed (no trailing slash)
VITE_CDN_URL=

# WebAuthn Configuration (for production)
# The RP (Relying Party) name and ID for WebAuthn
VITE_WEBAUTHN_RP_NAME="Vacay Photo Map"
VITE_WEBAUTHN_RP_ID=localhost
