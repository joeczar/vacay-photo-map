# Frontend Production Environment Variables
# These are embedded at BUILD TIME by Vite
# In CI: Set as GitHub repository variables (Settings > Secrets and variables > Actions > Variables)
# For local prod builds: Copy to .env.production and fill in values

# API Backend URL (must be accessible from user's browser)
VITE_API_URL=https://photos.joeczar.com

# Frontend URL (for redirects)
VITE_APP_URL=https://photos.joeczar.com

# WebAuthn Configuration
VITE_WEBAUTHN_RP_NAME=Vacay Photo Map
VITE_WEBAUTHN_RP_ID=photos.joeczar.com
